# 用户使用手册

### 1. 安装与启动

1. 确保所有文件位于同一目录：
   
   - `main.py`
   
   - `coverage.py`
   
   - `network.py`
   
   - `prediction.py`
   
   - `relationships.py`
   
   - （可选）`people_data.txt` 或 `repair.txt`（批量导入用）

2. 打开终端，运行：
   
   bash
   
   python main.py

### 2. 主菜单

启动后将显示：

text

==================================================
 社交网络分析工具箱（持久化版本）
==================================================

1. 创建人物字典 (create_person)
2. 修改人物字典 (update_person)
3. 删除人物字典 (delete_person)
4. 查找直接好友 (friend_besties)
5. 查找二度好友 (friend_second_besties)
6. 计算覆盖率 (besties_coverage)
7. 预测特征 (friendly_prediction)
8. 退出程序
   ==================================================

### 3. 功能详解

#### 3.1 创建人物（菜单1）

- **手动输入**：依次输入人名、特征（键=值，空行结束）、好友列表（每行一人名，空行结束）。

- **批量导入**：选择 `2`，输入文本文件路径，格式如下：
  
  text
  
  张三: university=南东大学,hobby=reading; 李四,王五
  李四: university=清北大学; 张三,赵六
  
  - 每行：`人名: 特征1=值1,特征2=值2; 好友1,好友2`
  
  - 分号前为特征，分号后为好友（可省略）
  
  - 支持注释行（`#`开头）和空行

#### 3.2 修改人物（菜单2）

- 先选择要修改的人名（从现有列表中选）。

- 可添加/修改特征、添加好友、删除好友。所有操作自动维护关系的双向性。

#### 3.3 删除人物（菜单3）

- 选择人名后删除，同时自动从所有人的好友列表中移除该人。

#### 3.4 查找直接好友（菜单4）

- 输入人名，返回其直接好友列表（排序后）。

#### 3.5 查找二度好友（菜单5）

- 返回朋友的朋友，不包含自身和直接好友。

#### 3.6 计算覆盖率（菜单6）

- 输入测试集人物（同一个人物用英文逗号分隔）。

- 选择关系函数组合：`0-无`（仅种子本身）、`1-仅直接好友`、`2-仅二度好友`、`3-两者组合`。

- 程序输出被覆盖的人数占总人数的比例。

#### 3.7 预测特征（菜单7）

- 输入目标人物和要预测的特征集（逗号分隔，如 `university,hobby`）。

- 算法：首先在直接好友中查找该特征值，采用多数投票（平局时按字母顺序列出全部）；若无直接好友有该特征，则使用二度好友；仍然无则返回空列表。

### 4. 数据持久化

- 所有人物、特征、好友关系自动保存在 `data.json` 中。该文件为 JSON 格式，可手动编辑（但注意保持结构）。

- 程序启动时自动加载，退出时自动保存。

### 5. 常见问题

- **添加好友时提示“不存在”**：需要先创建该好友人物，或使用修改功能批量创建。

- **预测特征结果为空**：目标人物的一度、二度好友中均无人具有该特征键。

- **覆盖率计算为0**：请检查测试集中的人物名称是否存在于数据中。

---

## 项目文件说明手册

| 文件名                              | 作用                                | 主要函数/类                                                       |
| -------------------------------- | --------------------------------- | ------------------------------------------------------------ |
| `main.py`                        | 程序入口，提供控制台菜单、数据管理（持久化）、文件导入、用户交互  | `DataManager`, `menu_*`, `import_people_from_file`           |
| `coverage.py`                    | 实现覆盖率计算逻辑和辅助包装函数                  | `besties_coverage`, `as_set`                                 |
| `network.py`                     | 从边列表构建友好字典（无向图）                   | `get_friendly_dict`                                          |
| `prediction.py`                  | 基于多数投票的特征预测算法（一度→二度）              | `friendly_prediction`                                        |
| `relationships.py`               | 查询一度好友和二度好友，包含双向邻居查找              | `friend_besties`, `friend_second_besties`, `_direct_friends` |
| `data.json`                      | 自动生成的持久化文件，存储所有人物、特征、好友关系（JSON格式） | —                                                            |
| `repair.txt` / `people_data.txt` | 示例批量导入文件（用户可自定义）                  | —                                                            |

### 文件依赖关系

text

main.py
  ├─ import_people_from_file → DataManager.add_person
  ├─ menu_friend_besties → relationships.friend_besties
  ├─ menu_friend_second_besties → relationships.friend_second_besties
  ├─ menu_besties_coverage → coverage.besties_coverage, coverage.as_set
  └─ menu_friendly_prediction → prediction.friendly_prediction

所有 `.py` 文件之间通过函数调用衔接，无循环依赖。
